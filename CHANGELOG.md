# v5

- PNG生成を SVG/foreignObject 経由から Canvas 直接描画へ変更
- 「PNG変換用SVGを読み込めませんでした」を根本回避
- ルビ、本文、箇条書き、手順番号を Canvas 上に直接描画
- 個別PNGとZIP一括保存が同一のPNG生成経路を使用
- 固定縦横比、配置、余白、2x/3x/4xを維持

# Changelog

## v4
- 一括ZIP保存の0KB問題を修正。
- 保存先を先に作る方式を廃止し、全PNG→ZIPをメモリ上で完全生成してから保存する2段階方式へ変更。
- 各PNGが0バイトでないこと、完成ZIPが空でないことを保存前に検証。
- ZIP生成完了後にファイル数と容量を表示。
- 「名前を付けてZIP保存」と「通常ダウンロード」を選択可能。
- File System Access API利用時はBlobではなくUint8Arrayを書き込み、互換性を向上。

## v3
- 縦横比・配置・余白をプレビューへ即時反映。
- 複数PNGの一括保存をZIP化。

## v2
- ブロックごとの縦横比・配置・余白設定を追加。
